 <%= form_for(@provider_review) do |f| %>  

  <% if @provider_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@provider_review.errors.count, "error") %> prohibited this provider_review from being saved:</h2>

      <ul>
      <% @provider_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <h1>Review for <%= @provider.name %></h1> 

  <div class="field">
    <%= f.label :reviewer %><br>
    <%= f.text_field :reviewer %>
  </div>
  
<!-- TODO Please figure out how to do this correctly -->
<%= hidden_field_tag(:provider_id, @provider.id) %>
  <div class="field">
    <%= f.label :provider %><br>
    <%= f.text_field :provider, :value => @provider.name, :disabled => true %>
  </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
